<h1>Expense Breakdown</h1>
<div id="expense_breakdown">
  <table>
    <tr>
      <th>Envelope</th>
      <th>Amount</th>
      <th>%</th>
    </tr>
  <% @envelopes.each do |e| %>
    <tr>
      <td><%= e.name %></td>
      <td><%= number_to_currency(e.expense) %></td>
      <td><%= number_to_percentage(e.expense/@total * 100, :precision => 0) %></td>
    </tr>
  <% end %>
 </table>
 <p>Total: <%= number_to_currency(@total) %></p>
</div>
